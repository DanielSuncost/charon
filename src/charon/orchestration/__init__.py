"""Durable orchestration primitives.

``fsm`` guards domain lifecycles, while ``graph_runtime`` provides declarative
workflow topology with branching, parallel readiness, joins, loops, and live
projection. ``runtime`` remains the compatibility scheduler for compact
code-defined steps. The daemon and interactive TUI both drive durable work.
"""
