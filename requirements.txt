# NOTE: pyproject.toml is the canonical dependency list.
# This file exists for CI and plain-pip installs; keep it in sync.
# Optional integrations are available through the memory, browser, office,
# and all extras declared in pyproject.toml.
httpx>=0.28
websockets>=15.0
