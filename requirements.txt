# NOTE: pyproject.toml is canonical for core dependencies and extras.
# This is the full-feature dependency list used by scripts/install-dev.sh, CI,
# and the release bundle. For a lightweight install, use `pip install .` and
# add the memory, browser, or office extras as needed.
httpx>=0.28
websockets>=15.0
sentence-transformers>=4.0,<5.0
sqlite-vec>=0.1.6
playwright>=1.50
openpyxl>=3.1
python-docx>=1.1
python-pptx>=1.0
