# NOTE: pyproject.toml is the canonical dependency list.
# This file exists for CI and plain-pip installs; keep it in sync.
httpx[http2]>=0.28
websockets>=15.0
playwright>=1.50
sqlite-vec>=0.1.6
sentence-transformers>=4.0,<5.0
openpyxl>=3.1
python-docx>=1.1
python-pptx>=1.0
