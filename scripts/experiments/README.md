# scripts/experiments

One-off research and evaluation harnesses (memory benchmarks, judge-loop
experiments, reward-hacking probes). Not product code — nothing in the
runtime imports from here. Run them from the repo root with the project
venv; outputs are written to `results/`, which is gitignored.
